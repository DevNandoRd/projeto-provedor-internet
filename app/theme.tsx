import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: '#002dad', // altere esta cor para a cor desejada
      },
      secondary: {
        main: '#ff6000', // altere esta cor para a cor desejada
      },
      // Outras cores personalizadas aqui, se necessário
    },
  });
  
  export default theme;